{
  "name": "reflomenu-parser",
  "version": "1.0.0",
  "description": "A parser to extract data from Reflo's activity list website to convert to JSON and CSV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "turndown": "^6.0.0"
  }
}
